import 'package:flutter/material.dart';

/// Option A — Forest (dark). Single source of truth for the design system.
abstract final class AppColors {
  static const background = Color(0xFF0F1C14);
  static const surface = Color(0xFF1A2E1F);
  static const surfaceLight = Color(0xFF243B28);
  static const primary = Color(0xFF4ADE80);
  static const textPrimary = Color(0xFFF0FDF4);
  static const textSecondary = Color(0xFF86EFAC);
  static const teluguColor = Color(0xFFFFFFFF);
  static const chipBg = Color(0xFF1F3527);
  static const borderGlass = Color(0x14FFFFFF);
  static const splashBackground = Color(0xFF0F1C14);
  static const shadowCard = Color(0x0F000000);
}
