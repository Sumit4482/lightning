PORT=3000
# Comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:4200,https://your-site.netlify.app

# Secret for POST /api/admin/daily (use same value on publish page)
ADMIN_SECRET=your-long-random-secret-here
